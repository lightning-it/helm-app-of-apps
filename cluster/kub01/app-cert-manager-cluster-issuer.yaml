---
issuer:
  vault:
    enabled: false   # oder true wenn du beides willst

  letsencrypt:
    enabled: true
    name: letsencrypt-staging
    email: deg@l-it.io
    server: https://acme-staging-v02.api.letsencrypt.org/directory

css:
  enabled: true
  name: hashicorp-vault
  cloudflareSecretPath: kub01/cloudflare
  cloudflareSecretProperty: api-token

certificate:
  enabled: true
  issuerName: letsencrypt-staging
  dnsNames:
    - "*.apps.kub01.poc01.cloud.l-it.io"
